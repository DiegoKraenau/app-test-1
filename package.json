{
  "name": "app-test-1",
  "version": "1.0.9",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit:patch": "npm version patch --no-git-tag-version --force",
    "commit:minor": "npm version minor --no-git-tag-version --force",
    "commit:major": "npm version major --no-git-tag-version --force",
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.14",
    "express": "^4.18.1",
    "husky": "^8.0.1"
  },
  "husky": {
    "hooks": {
      "prepare-commit-msg": "node scripts/validatebranch.js && node scripts/commitmsg.js"
    }
  }
}
